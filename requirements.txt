# Core LatentSync dependencies (as seen in your pip freeze)
torch==2.5.1+cu121
torchvision==0.20.1+cu121 # Often goes with torch
numpy==1.26.4
omegaconf==2.3.0
diffusers==0.32.2 # Used by LatentSync itself
accelerate==0.26.1 # Often goes with diffusers
onnx==1.17.0
onnxruntime-gpu==1.21.0
opencv-python==4.9.0.80 # Or opencv-python-headless if you don't need GUI
# easydict==1.13 # Check if explicitly used by LatentSync

# Core Gradio Web UI and related functionalities
gradio==5.24.0 # Your installed version
transformers==4.48.0 # Your installed version
librosa==0.10.1 # Your installed version
pandas==2.3.0 # Your installed version
Pillow==11.3.0 # Your installed version (important for image processing)
# Explicitly including anyio as it's a critical underlying for Gradio async
anyio==4.9.0 # Your installed version

# General utilities/dependencies that are likely needed by the above
# (You can remove these if pip install -r requirements.txt works without them,
# as they are often sub-dependencies that pip automatically resolves)
scipy==1.15.3 # Often a dependency of librosa
soundfile==0.13.1 # Often a dependency of librosa
scikit-learn==1.7.0 # If used by LatentSync or some underlying component

# Optional, but sometimes useful:
# ffmpeg-python==0.2.0 # If you directly use ffmpeg-python in your script
# uvicorn==0.35.0 # If you run Gradio specifically with uvicorn (Gradio uses it internally)
# httpx==0.28.1 # If you have network calls outside Gradio's internal ones (Gradio uses it)
# rich==14.0.0 # For rich text formatting in terminals, not strictly necessary for app function